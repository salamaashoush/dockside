name: Release

on:
  push:
    tags:
      - 'v*'

env:
  CARGO_TERM_COLOR: always

jobs:
  build-macos:
    name: Build macOS App
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: macos-aarch64-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            macos-aarch64-cargo-

      - name: Build
        run: cargo build --release --target aarch64-apple-darwin

      - name: Generate icon if missing
        run: |
          if [[ ! -f "assets/AppIcon.icns" ]]; then
            chmod +x scripts/generate-placeholder-icon.sh
            ./scripts/generate-placeholder-icon.sh
          fi

      - name: Create app bundle
        run: |
          VERSION=${GITHUB_REF#refs/tags/}

          # Create bundle structure
          mkdir -p Dockside.app/Contents/MacOS
          mkdir -p Dockside.app/Contents/Resources

          # Copy binary
          cp target/aarch64-apple-darwin/release/dockside Dockside.app/Contents/MacOS/

          # Copy Info.plist and update version
          cp assets/Info.plist Dockside.app/Contents/
          VERSION_NUM=${VERSION#v}
          sed -i '' "s/0.1.0/$VERSION_NUM/g" Dockside.app/Contents/Info.plist

          # Copy icon if exists
          if [[ -f "assets/AppIcon.icns" ]]; then
            cp assets/AppIcon.icns Dockside.app/Contents/Resources/
          fi

          # Copy themes
          cp -R themes Dockside.app/Contents/Resources/

          # Package
          zip -r Dockside-$VERSION-macos-arm64.zip Dockside.app

      - name: Upload app bundle
        uses: actions/upload-artifact@v4
        with:
          name: Dockside-macos-arm64
          path: Dockside-*.zip

  build-linux:
    name: Build Linux App
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgtk-3-dev \
            libwebkit2gtk-4.1-dev \
            libxdo-dev \
            libayatana-appindicator3-dev \
            libxcb-shape0-dev \
            libxcb-xfixes0-dev \
            libxkbcommon-dev \
            libssl-dev \
            libvulkan-dev \
            libasound2-dev

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: linux-x86_64-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            linux-x86_64-cargo-

      - name: Build
        run: cargo build --release --target x86_64-unknown-linux-gnu

      - name: Create Linux package
        run: |
          VERSION=${GITHUB_REF#refs/tags/}

          # Create package directory
          mkdir -p dockside-$VERSION-linux-x86_64

          # Copy binary
          cp target/x86_64-unknown-linux-gnu/release/dockside dockside-$VERSION-linux-x86_64/

          # Copy themes
          cp -R themes dockside-$VERSION-linux-x86_64/

          # Copy assets
          mkdir -p dockside-$VERSION-linux-x86_64/assets
          cp assets/icon.png dockside-$VERSION-linux-x86_64/assets/

          # Create desktop entry
          cat > dockside-$VERSION-linux-x86_64/dockside.desktop << EOF
          [Desktop Entry]
          Name=Dockside
          Comment=Docker Management Desktop App
          Exec=dockside
          Icon=dockside
          Type=Application
          Categories=Development;Utility;
          EOF

          # Create README
          cat > dockside-$VERSION-linux-x86_64/README.md << EOF
          # Dockside for Linux

          ## Installation

          1. Extract this archive
          2. Copy the \`dockside\` binary to /usr/local/bin or ~/.local/bin
          3. Copy the \`themes\` directory to ~/.config/dockside/themes
          4. (Optional) Copy dockside.desktop to ~/.local/share/applications/

          ## Requirements

          - Docker installed and running
          - Or Colima for VM-based Docker
          EOF

          # Package
          tar -czvf Dockside-$VERSION-linux-x86_64.tar.gz dockside-$VERSION-linux-x86_64

      - name: Upload Linux package
        uses: actions/upload-artifact@v4
        with:
          name: Dockside-linux-x86_64
          path: Dockside-*.tar.gz

  build-windows:
    name: Build Windows App
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: windows-x86_64-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            windows-x86_64-cargo-

      - name: Build
        run: cargo build --release --target x86_64-pc-windows-msvc

      - name: Create Windows package
        shell: pwsh
        run: |
          $VERSION = $env:GITHUB_REF -replace 'refs/tags/', ''

          # Create package directory
          New-Item -ItemType Directory -Force -Path "Dockside-$VERSION-windows-x86_64"

          # Copy binary
          Copy-Item "target/x86_64-pc-windows-msvc/release/dockside.exe" "Dockside-$VERSION-windows-x86_64/"

          # Copy themes
          Copy-Item -Recurse "themes" "Dockside-$VERSION-windows-x86_64/"

          # Copy assets
          New-Item -ItemType Directory -Force -Path "Dockside-$VERSION-windows-x86_64/assets"
          Copy-Item "assets/icon.png" "Dockside-$VERSION-windows-x86_64/assets/"

          # Create README
          @"
          # Dockside for Windows

          ## Installation

          1. Extract this archive
          2. Run dockside.exe

          ## Requirements

          - Docker running in WSL2 with TCP port exposed
          - Or Docker Desktop installed

          ## WSL2 Docker Setup

          1. Install Docker in your WSL2 distro (e.g., Ubuntu)
          2. Edit /etc/docker/daemon.json to add:
             {"hosts": ["unix:///var/run/docker.sock", "tcp://0.0.0.0:2375"]}
          3. Restart Docker: sudo systemctl restart docker
          4. Dockside will automatically detect and connect

          "@ | Out-File -FilePath "Dockside-$VERSION-windows-x86_64/README.md" -Encoding utf8

          # Package
          Compress-Archive -Path "Dockside-$VERSION-windows-x86_64" -DestinationPath "Dockside-$VERSION-windows-x86_64.zip"

      - name: Upload Windows package
        uses: actions/upload-artifact@v4
        with:
          name: Dockside-windows-x86_64
          path: Dockside-*.zip

  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: [build-macos, build-linux, build-windows]
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List artifacts
        run: find artifacts -type f

      - name: Extract changelog for version
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}

          # Extract the section for this version from CHANGELOG.md
          if [[ -f "CHANGELOG.md" ]]; then
            awk "/^## \[$VERSION\]/{found=1; next} /^## \[/{found=0} found" CHANGELOG.md > changelog.md

            if [[ -s changelog.md ]]; then
              {
                echo ""
                echo "## Installation"
                echo ""
                echo "### macOS (Apple Silicon)"
                echo ""
                echo "Download \`Dockside-vX.X.X-macos-arm64.zip\`, extract, and move \`Dockside.app\` to \`/Applications\`."
                echo ""
                echo "### Linux (x86_64)"
                echo ""
                echo "Download \`Dockside-vX.X.X-linux-x86_64.tar.gz\`, extract, and run \`./dockside\`."
                echo ""
                echo "### Windows (x86_64)"
                echo ""
                echo "Download \`Dockside-vX.X.X-windows-x86_64.zip\`, extract, and run \`dockside.exe\`."
                echo ""
                echo "**Note:** Windows requires Docker running in WSL2 with TCP port exposed. See the included README for setup instructions."
                echo ""
                echo "### Build from Source"
                echo ""
                echo '```bash'
                echo "git clone https://github.com/salamaashoush/dockside"
                echo "cd dockside"
                echo "cargo build --release"
                echo '```'
              } >> changelog.md
            else
              echo "## What's Changed" > changelog.md
              echo "" >> changelog.md
              git log --pretty=format:"- %s" $(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")..HEAD 2>/dev/null | head -20 >> changelog.md || echo "- Initial release" >> changelog.md
            fi
          else
            echo "## What's Changed" > changelog.md
            echo "" >> changelog.md
            git log --pretty=format:"- %s" -20 >> changelog.md
          fi

          echo "Generated release notes:"
          cat changelog.md

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          body_path: changelog.md
          files: |
            artifacts/**/*.zip
            artifacts/**/*.tar.gz
          draft: false
          prerelease: ${{ contains(github.ref, 'alpha') || contains(github.ref, 'beta') || contains(github.ref, 'rc') }}
